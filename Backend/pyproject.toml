[project]
name = "resumeanalyzerrag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Beeki957710", email = "dbishal536@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain-groq>=1.1.3",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]

[project.scripts]
resumeanalyzerrag = "resumeanalyzerrag:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
